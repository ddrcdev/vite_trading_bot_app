import json
import sys
import pandas as pd 

if __name__ == "__main__":

    print(json.dumps("Este es un texto de prueba desde Python"))
    # # Leer los datos de kline desde el archivo temp.json
    # with open(sys.argv[1], 'r') as file:
    #     data = json.load(file)

    # kline_data = data
    # print(json.dumps(kline_data))
